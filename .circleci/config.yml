version: 2
jobs:
  build:
    docker:
      - image: phusion/baseimage:0.11
        environment:
          DEBUG_BUILD: true

    working_directory: ~/edgeware

    steps:
      - checkout
      - run: apt-get update

      # module test cases
      - run: cd ~/edgeware && source ./.cargo/env && ./scripts/test.sh
